-- SET AUTOCOMMIT = ON;

-- Create posts db
-- SELECT 'CREATE DATABASE posts; CREATE USER posts; GRANT ALL PRIVILEGES ON DATABASE posts TO posts;'
-- WHERE NOT EXISTS (SELECT FROM pg_database WHERE datname = 'posts')\gexec

-- Create charts db
-- SELECT 'CREATE DATABASE charts; CREATE USER charts; GRANT ALL PRIVILEGES ON DATABASE charts TO charts;'
-- WHERE NOT EXISTS (SELECT FROM pg_database WHERE datname = 'charts')\gexec
